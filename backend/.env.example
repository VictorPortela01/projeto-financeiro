# URL de conexão do MongoDB Atlas (substituir com seus dados)
MONGO_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# Porta do servidor
PORT=5000

# Segredos do JSON Web Token (JWT)
JWT_SECRET=sua_chave_secreta_para_access_token_aqui
JWT_REFRESH_SECRET=sua_chave_secreta_para_refresh_token_aqui

# Tempo de vida dos tokens
JWT_LIFETIME=15m
JWT_REFRESH_LIFETIME=7d